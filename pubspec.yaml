name: s_network_connection_checker
description: "Check if you are connected to the internet."
version: 0.0.1
homepage: https://github.com/sbaskoy/s_network_connection_checker
repository: "https://github.com/sbaskoy/s_network_connection_checker"

author: "Salim Baskoy"
publish_to: none

topics:
  - network
  - connectivity
  - ping

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.2.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  s_state: ^0.0.1+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:


